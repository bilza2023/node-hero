<%- include('../partials/header') %>
<%- include('../partials/flash') %>

<style>
  body {
    background-color: #181a1f;
    color: #f0f0f0;
    font-family: 'Inter', sans-serif;
    padding: 2rem;
  }

  h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: #8be9fd;
  }

  .topbar {
    margin-bottom: 2rem;
  }

  .topbar a {
    background-color: #7fdbca;
    color: #181a1f;
    padding: 0.6rem 1.2rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: bold;
    transition: background 0.2s;
  }

  .topbar a:hover {
    background-color: #a0f1df;
  }

  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  .card {
    background-color: #22252a;
    padding: 1.2rem;
    border-radius: 10px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.3);
    transition: all 0.2s ease-in-out;
    text-decoration: none;
    color: inherit;
    display: block;
    border: 1px solid #333;
  }

  .card:hover {
    background-color: #2e323a;
    transform: translateY(-3px);
    box-shadow: 0 4px 10px rgba(0,0,0,0.4);
  }

  .card-title {
    font-size: 1.2rem;
    font-weight: bold;
    margin-bottom: 0.3rem;
    color: #8be9fd;
  }

  .card-sub {
    font-size: 0.95rem;
    color: #b5b5b5;
  }
</style>

<h1>📘 All Syllabi (Tcodes)</h1>

<div class="topbar">
  <a href="/syllabus/new">➕ Create New Syllabus</a>
</div>

<div class="card-grid">
  <% for (const t of tcodes) { %>
    <a class="card" href="/syllabus/<%= t.tcodeName %>">
      <div class="card-title"><%= t.tcodeName %></div>
      <div class="card-sub"><%= t.title %></div>
    </a>
  <% } %>
</div>
